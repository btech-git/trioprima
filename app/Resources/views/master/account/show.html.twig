{% extends 'master/layout.html.twig' %}

{% block title 'Show Account' %}

{% block actions %}
    {{ include('master/actions.html.twig') }}
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ account.code|default }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ account.name|default }}</td>
            </tr>
            <tr>
                <th>Is Active</th>
                <td>{{ account.isActive|default ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
