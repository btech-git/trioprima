{% extends 'master/layout.html.twig' %}

{% block title 'Show Supplier' %}

{% block actions %}
    {{ include('master/actions.html.twig') }}
{% endblock %}

{% block content %}
    <table class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ supplier.code|default }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ supplier.name|default }}</td>
            </tr>
            <tr>
                <th>Company</th>
                <td>{{ supplier.company|default }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ supplier.address|default }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ supplier.phone|default }}</td>
            </tr>
            <tr>
                <th>Fax</th>
                <td>{{ supplier.fax|default }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ supplier.email|default }}</td>
            </tr>
            <tr>
                <th>Tax Number</th>
                <td>{{ supplier.taxNumber|default }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ supplier.note|default }}</td>
            </tr>
            <tr>
                <th>Is Active</th>
                <td>{{ supplier.isActive|default ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
